#### Build image
```bash
$ docker build --rm=true -t adhocguru/app:tag .
```

#### Push image
```bash
$ docker push adhocguru/app:tag
```

#### Pull image
```bash
$ docker pull adhocguru/app:tag
```

#### Run local stack
```bash
$ docker-compose up -d
```